cmake_minimum_required(VERSION 3.21 FATAL_ERROR)
include(FetchContent)

FIND_VULKAN_OR_FAIL()

FetchContent(
    glslang
    GIT_REPOSITORY https://github.com/KhronosGroup/glslang.git
)

set(core_libs SavannaVk SavannaIO SavannaCore)
set(core_include_dirs
    ${CMAKE_SOURCE_DIR}/Modules/Core
    ${CMAKE_SOURCE_DIR}/Modules/Gfx/Vulkan)

set(external_libs ${VULKAN_LIB_LIST} ${HELPER_LIB_LIST})
set(external_include_dirs "")
DECLARE_CORE_LIBRARY(SavannaSpirVShaderCompiler "${core_libs}" "${core_include_dirs}" "${external_libs}" "${external_include_dirs}")
