# External Folder Exclusion Overrides

bin
[Aa]rtifacts/*
![Aa]rtifacts/DONOTDELETE.txt

# TODO @DavidMohrhardt: Move extracted artifacts to a subfolder and exclude that folder from the repository.
cmake*
fmt*
glfw*
glm*
imgui*
ninja*
python*
[Vv]ulkan*

# Allow the submodules folder to persist in the repository
![Ss]ubmodules

# Allow build dependencies to persist in the repository
!BuildDependencies.json

# Allow hidden folders to persist in the repository
!*~/